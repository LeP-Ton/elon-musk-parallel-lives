# 项目整体认知

## 基本信息
《马斯克传：平行人生》是 GameX 系统型互动小说。前台像小说，后台像模拟器；玩家选择、人物惯性与可复现随机共同影响人生。

## 技术与架构
- Web-first：React、TypeScript、Vite，使用 Sites 的 Vinext 路由适配并静态导出；纯函数叙事引擎与 React 界面解耦。需 Node.js >=22.13。
- 声明式内容包通过 Registry 注册，条件驱动的状态图允许分支、汇合和横向转向。
- 人物模型、内容包、存档均版本化。随机按世界种子、用途、事件和尝试序号隔离。
- 历史事实、戏剧化历史和平行推演明确区分；叙事原创，史料可追溯。
- 第一阶段交付 10～15 个核心事件；1971～2002 完整 V1 与深度事业模块留待后续阶段。

## 运行与验证
- `pnpm dev`：本地游戏。
- `pnpm build`：类型检查和生产构建。
- `pnpm test`：引擎、内容、存档、路线验收。
- `pnpm validate:content`：内容完整性校验。

## 工作约定
- 沟通、文档及代码注释使用中文；回复以“会话-N：”开头。
- 开始工作时检查 Git 分支；与上一会话不同需提示并确认。
- 每次代码变更新建带秒级时间前缀的 `.agentdocs/workflow/` 文档，完整记录代码 diff 和测试。
- 先读 `.agentdocs/index.md`，再按需读取具体文档；不全量检索 workflow。
- 根 AGENTS.md 只记录整体与核心认知，具体需求与迭代记录放在 `.agentdocs`。
- 保留用户现有修改；通过变更文档辨识会话版本，不盲目回滚。

## 当前版本基线
- 会话-1 从空目录开始，初始不存在 Git 分支；现已初始化独立 `main` 分支。
- 需求原文保存在 `plan/requirements-original.md`，不得改写；执行计划见 `plan/implementation.md`。
