# 项目整体认知

## 基本信息
《马斯克传：平行人生》是 GameX 系统型互动小说。前台像小说，后台像模拟器；玩家选择、人物惯性与可复现随机共同影响人生。

## 技术与架构
- Web-first：React、TypeScript、Vite，使用 Sites 的 Vinext 路由适配并静态导出；纯函数叙事引擎与 React 界面解耦。需 Node.js >=22.13。
- 声明式内容包通过 Registry 注册，条件驱动的状态图允许分支、汇合和横向转向。
- 人物模型、内容包、存档均版本化。随机按世界种子、用途、事件和尝试序号隔离。
- 手动与自动游玩共用 `choose / advance` 状态转移入口；自动选择策略有独立版本和随机用途，不预演结果或绕过条件。
- 视觉小说采用两层状态图：场内旁白/对白/选择/条件节点，场景结束才调度下一幕和推进年代。稳定节点ID、已读日志和选择账本共同支持复现。
- 人物关系与承诺进入纯函数条件和效果；人物年龄/表情由资源清单管理，演出不能直接修改人生数据。
- 新版存档使用 schemaVersion 2 与独立 `parallel-lives:v2:`；旧 `v1` 只读备份，不迁移、不覆盖。新版读档重放种子与选择校验游标和效果，开发检查器保持只读。
- 播放、倍速和页面计时属于临时界面状态，不写入人生存档；接管、读档、面板打开和进入后台时停止自动推进。
- 历史事实、戏剧化历史和平行推演明确区分；叙事原创，史料可追溯。
- 当前内容为1983～1999的六章视觉小说；真实背景与原创场景分开标识，2000年后的深度事业模块不在此篇范围。旧摘要内容只作为引擎回归夹具，不是用户入口。

## 运行与验证
- `pnpm dev`：本地游戏。
- `pnpm build`：类型检查和生产构建。
- `npm run build:pages`：GitHub Pages 专用静态构建与资源路径校验，输出 `dist/github-pages`。
- `pnpm test`：引擎、内容、存档、路线验收。
- `pnpm validate:content`：内容完整性校验。
- `node --import tsx scripts/novel-report.ts`：统计1000条新版人生的场景、汉字量与结局分布；不能替代真人阅读时长验收。

## 工作约定
- 沟通、文档及代码注释使用中文；回复以“会话-N：”开头。
- 开始工作时检查 Git 分支；与上一会话不同需提示并确认。
- 每次代码变更新建带秒级时间前缀的 `.agentdocs/workflow/` 文档，完整记录代码 diff 和测试。
- 先读 `.agentdocs/index.md`，再按需读取具体文档；不全量检索 workflow。
- 根 AGENTS.md 只记录整体与核心认知，具体需求与迭代记录放在 `.agentdocs`。
- 保留用户现有修改；通过变更文档辨识会话版本，不盲目回滚。

## 当前版本基线
- 会话-1 从空目录开始，初始不存在 Git 分支；现已初始化独立 `main` 分支。
- 需求原文保存在 `plan/requirements-original.md`，不得改写；执行计划见 `plan/implementation.md`。

## 多平台发布
- 保留 Sites 配置与既有部署；GitHub Pages 是独立发布渠道，不自动更新 Sites。
- 根路径构建用于 Sites；Pages 构建通过 `NEXT_PUBLIC_BASE_PATH` 适配项目子目录，内容包资源路径不随平台改写。
- GitHub 的 `main` 推送触发测试、内容校验、静态构建与 Pages 发布；只上传静态产物，不上传源码目录作为网站。
- 存档按浏览器域名隔离；跨 Sites 与 Pages 搬迁进度需导出再导入。
